# ==================================================
# PRODUCTION PROFILE
# ==================================================

# MongoDB Atlas Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://admin:admin%40vamshi@cluster0.apxjwes.mongodb.net/meetrivo?retryWrites=true&w=majority}
spring.data.mongodb.database=${MONGODB_DATABASE:meetrivo}
spring.mongodb.uri=${MONGODB_URI:mongodb+srv://admin:admin%40vamshi@cluster0.apxjwes.mongodb.net/meetrivo?retryWrites=true&w=majority}
spring.mongodb.database=${MONGODB_DATABASE:meetrivo}

# ==================================================
# JWT CONFIGURATION
# ==================================================
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# ==================================================
# STORAGE CONFIGURATION
# ==================================================
meetrivo.storage.provider=local
meetrivo.storage.local-path=${STORAGE_PATH:./recordings}
meetrivo.storage.max-file-size-mb=500

# ==================================================
# REDIS CONFIGURATION
# ==================================================
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}

# Enable Redis in production
# spring.data.redis.host defaults to localhost but can be overridden by env variable REDIS_HOST

# ==================================================
# SMTP MAIL CONFIGURATION
# ==================================================
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==================================================
# LOGGING
# ==================================================
logging.level.com.meetrivo=INFO
logging.level.org.springframework.web=WARN
logging.level.org.springframework.security=INFO
logging.level.org.springframework.data.mongodb=INFO

# ==================================================
# ACTUATOR
# ==================================================
management.endpoints.web.exposure.include=health,metrics,info,env
management.endpoint.health.show-details=always

# ==================================================
# GROQ AI CONFIGURATION
# ==================================================
groq.api.key=${GROQ_API_KEY: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}
groq.model=${GROQ_MODEL:llama3-8b-8192}
groq.max-tokens=${GROQ_MAX_TOKENS:2048}
groq.temperature=${GROQ_TEMPERATURE:0.3}

# ==================================================
# FILE UPLOAD CONFIGURATION
# ==================================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# ==================================================
# BACKUP CONFIGURATION
# ==================================================
meetrivo.backup.path=${BACKUP_PATH:./backups}

# ==================================================
# APP URL CONFIGURATIONS
# ==================================================
app.frontend.url=${FRONTEND_URL:https://themeetrivo.vercel.app}
app.backend.url=${BACKEND_URL:https://meetrivo.onrender.com}
app.meeting.join-url-prefix=${FRONTEND_URL:https://themeetrivo.vercel.app}/join/